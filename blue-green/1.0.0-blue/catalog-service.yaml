apiVersion: v1
kind: Service
metadata:
  labels:
    app: catalog
    version: blue
  name: catalog-blue
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: catalog
    version: blue
