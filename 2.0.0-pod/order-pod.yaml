# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.0.3
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2024-05-25T14:05:55Z"
  labels:
    app: order
  name: order
spec:
  containers:
  - image: registry.gitlab.com/ch-mc-b/autoshop-ms/app/shop/order:2.0.0
    name: order
    securityContext: {}

